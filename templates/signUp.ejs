<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form</title>
    <style>
        body {
            background-color: #2d3748;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            padding: 0;
        }

        form {
          color: white;
            background-color: transparent;
            padding: 20px;
            border-radius: 0.25rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            width: 100%;
            max-width: 48rem;
            display: flex;
            flex-direction: column; /* Single column to control layout in each section */
        }

        .form-section {
            display: flex;
            flex-wrap: wrap; /* Allows wrapping for smaller screens */
            justify-content: space-between; /* Ensures space between inputs */
            margin-bottom: 20px; /* Space between sections */
        }

        h2 {
            color: white;
            font-size: 1.5rem;
            margin-bottom: 10px; /* Smaller margin for header */
        }

        .input-group {
            color: white;
            display: flex;
            width: 48%; /* Adjust width for side by side alignment */
            flex-direction: column;
            margin-bottom: 10px; /* Consistent margin for all input groups */
        }

      .input-group input::placeholder {
          color: white; /* Sets placeholder text color to white */
      }

      
        input, select {
            color: white;
            background-color: transparent;
            border: 2px solid #e2e8f0;
            padding: 0.5rem;
            /* width: 95%; /* Full width within input group */ 
            border-radius: 0.25rem;
        }

        button {
            background-color: #3182ce;
            color: white;
            font-weight: bold;
            padding: 0.5rem 1rem;
            border-radius: 0.25rem;
            text-align: center;
            border: none;
            cursor: pointer;
            width: auto; /* Auto width for button */
        }
      
        input::placeholder, input:-ms-input-placeholder, input::-ms-input-placeholder { 
            color: white; /* White placeholder text */
        }

        button:hover, button:focus {
            background-color: #2c5282;
            outline: none;
        }
    </style>
</head>
<body>
    <div>
        <form action="/submitForm" method="POST">
          <h2>Basic Information</h2>
            <div class="form-section">
                <div class="input-group"><input type="text" name="name" placeholder="Name" id="basic_name" required></div>
                <div class="input-group"><input type="password" name="password" placeholder="Password" id ="password" required></div>
                <div class="input-group"><input type="number" name="age" min="15" max="90" placeholder="Age" id="basic_age" required></div>
                <div class="input-group"><input type="email" name="email" placeholder="Email" id="basic_email" required></div>
                <div class="input-group"><input type="tel" name="phoneNumber" placeholder="Phone Number" id="basic_phone_number" required></div>
                <div class="input-group"><input type="date" name="dob" placeholder="Date of Birth" id="birth_date" required></div>
                <div class="input-group"><input type="number" name="housingPriceRange" placeholder="Housing Price Range" step="100" id="housing_price"></div>
                <div class="input-group"><input type="text" name="locationPreference" placeholder="Location Preference" id="location_pref"></div>
                <div class="input-group"><input type="text" name="blurb" placeholder="About Me"></div>
                <div class="input-group"><select name="gender">
                    <option value="">Gender</option>
                    <option value="female">Female</option>
                    <option value="male">Male</option>
                    <option value="mix">Other</option>
                </select></div>
            </div>
          <!-- <div class="input-group2"><input type="text" name="blurb" placeholder="About Me"></div> -->
          <h2>Roommate Preferences</h2>
            <div class="form-section">
             
                <div class="input-group"><input type="number" name="roommateMinimumAge" placeholder="Preferred Minimum Roommate Age" id ="min_age" required></div>
                <div class="input-group"><input type="number" name="roommateMaximumAge" placeholder="Preferred Maximum Roommate Age" id="max_age" required></div>
                <div class="input-group"><select name="dailyRoutine">
                    <option value="">Select Routine</option>
                    <option value="early riser">I wake up early</option>
                    <option value="late riser">I can be classified as a night owl</option>
                    <option value="inconsistent">Inconsistent</option>
                </select></div>
                <div class="input-group"><select name="gender">
                    <option value="">Gender</option>
                    <option value="female">Females only</option>
                    <option value="male">Males only</option>
                    <option value="mix">Mixed</option>
                </select></div>
                <div class="input-group"><select name="foodPreference">
                    <option value="">Food</option>
                    <option value="vegan">Vegan</option>
                    <option value="vegetarian">Vegetarian</option>
                    <option value="non-veg">Non-Vegetarian</option>
                </select></div>
                <div class="input-group"><select name="pets">
                    <option value="">Pets</option>
                    <option value="allowed">Allowed</option>
                    <option value="not">Not Allowed</option>
                </select></div>
                <div class="input-group"><select name="guestFrequency">
                    <option value="">How often can guests visit?</option>
                    <option value="once">Once in a while</option>
                    <option value="always">Anytime</option>
                </select></div>
                <div class="input-group"><select name="smoking">
                    <option value="">Smoking</option>
                    <option value="yes">Yes. I smoke as well</option>
                    <option value="no">No. I can't handle second hand smoke.</option>
                </select></div>
                <button type="submit">Submit</button>
            </div>
        </form>
    </div>
</body>
</html>